import { createAppContainer } from "react-navigation";
//import HomeStack from "./homeStack";
//export default createAppContainer(HomeStack);

import HomeSwitch from "./homeSwitch";
export default createAppContainer(HomeSwitch);
